{
  "name": "demos",
  "private": true,
  "type": "module",
  "workspaces": [
    "astro",
    "native-testing"
  ],
  "scripts": {
    "start": "npm run dev -w demos-site",
    "dev": "npm run dev -w demos-site",
    "build": "npm run build -w demos-site",
    "preview": "npm run preview -w demos-site",
    "storybook": "npm run storybook -w demos-site",
    "build-storybook": "npm run build-storybook -w demos-site",
    "typecheck": "npm run typecheck -w demos-site",
    "test": "npm run test -w native-node-testing"
  },
  "devDependencies": {
    "@scottnath/devx": "^1.2.1",
    "typescript": "^6.0.3"
  },
  "engines": {
    "node": ">=24.0.0"
  }
}
